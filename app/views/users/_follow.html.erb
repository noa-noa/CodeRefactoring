<%= form_for(current_user.follows.build(followed_id: @user.id), remote: true, :authenticity_token => true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>