<%= form_for(current_user.follows.find_by(followed_id: @user.id), html: { method: :delete }, remote: true, :authenticity_token => true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>